{
  "buildCommand": "mkdir -p data && cp ../data/*.composites_recomputed.jsonl ../data/catalog_climate_profiles.jsonl data/ && npm run build",
  "devCommand": "npm run dev",
  "installCommand": "npm install",
  "framework": "nextjs",
  "outputDirectory": ".next"
}

